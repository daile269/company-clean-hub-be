package com.company.company_clean_hub_be.entity;

public enum AssignmentScope {
    CONTRACT,  // Phân công cho hợp đồng khách hàng (có contract)
    COMPANY    // Phân công nội bộ công ty (không có contract)
}
